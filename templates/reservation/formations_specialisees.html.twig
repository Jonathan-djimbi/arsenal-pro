{% extends 'base.html.twig' %}

{% block title %}<title>Formations spécialisées</title>{% endblock %}
{% block annonce %} {% endblock %}
{% block content %}

<div>
    <h3 class="text-center">[🏗️ Page bientôt disponible 🏗️]</h3><br><br>
</div>

<div class="d-flex mb-5 mt-5 container">
    <div class="row justify-content-center"> 
    <div class="col-md-4 col-sm-10">
        <h1 class="font-weight-bold">FORMATIONS SPÉCIALISÉES</h1><br>
    </div>  
    <div class="col-1"></div>  
    <div class="col-md-4 col-sm-10">
        <p>Vous pouvez choisir de suivre des formations spéciales aux côtés d'ARSENAL PRO et effectuer une réservation directement sur notre site internet.</p>
    </div>
    </div>
</div>
<hr>
<br>
<div>
    <div>
        <h2 class="font-weight-bold text-center">NOS FORMATIONS :</h2><br><br>
    </div>
    <div class="container">
        <div class="row">
            {% for formation in formations %}
            <div class="d-flex col-8 formationsDiv">
                <div>
                    <img src="{{ asset('uploads/'~ formation.image) }}" class="responsive-img" alt="photo arme formation arsenal pro"/>
                    <h4 class="font-weight-bold">{{ formation.name }}</h4><br>
                    <hr> <p>{{(( formation.price /100)|number_format(2)) }} €</p>  
                    <a href="{{ path('app_reservation_activite', {'id': formation.id, 'name': formation.name}) }}" title="{{formation.name}}" class="btn btn-primary btn-success">JE RÉSERVE</a>
                </div>
                <div class="w-100 texte-formation">
                    <strong>{{ formation.descriptionUn }}</strong><br><br>
                    {{ formation.descriptionDeux }}
                </div>
            </div>
            {% endfor %}
        </div>
    </div><br><hr><br>

{% endblock %}
