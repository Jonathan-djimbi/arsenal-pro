{% extends 'base.html.twig' %}

{% block title %}<title>Réglement générale de la protection des Données | Arsenal Pro</title>{% endblock %}
{% block annonce %} {% endblock %}
{% block content %}
{{ render(controller('App\\Controller\\HomeController::banniereShow',{ 'titre': "Réglement générale de la protection des Données",'section' : 0, 'multi' : false })) }}
<row class="">
    <hr>
    <br/>
    <article class="border text-justify">
        <div>
        {% for condition in rgpd %}
            <div>
                <h4>{{condition.question|upper}}</h4>
                <p>{{condition.reponse|raw}}</p>
            </div>
        {% endfor %}
        </div>
    </article>
</row>

{% endblock %}
